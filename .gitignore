.DS_Store
.claude/

# Source company profile — contains personnel bios the client asked to keep
# off the site; must not be published in a public repo.
*.pdf

# Build brief — kept locally, deliberately not published to the public repo.
CLAUDE.md
